[project]
name = "{{project_name}}"
version = "0.1.0"
description = "A serverless function for Functions.do"
requires-python = ">=3.11"

[tool.py2wasm]
# Python to WASM compilation settings for Cloudflare Workers

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
