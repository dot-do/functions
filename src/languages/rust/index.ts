/**
 * functions.do/rust
 *
 * Rust to WebAssembly compilation for Functions.do
 *
 * STATUS: NOT YET SUPPORTED
 * The compile function will throw a clear error explaining that
 * Rust compilation is not yet available.
 */

export { compileRust, type CompileRustOptions, type CompileRustResult } from './compile'
