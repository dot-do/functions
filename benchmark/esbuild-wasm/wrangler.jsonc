{
  "$schema": "../../node_modules/wrangler/config-schema.json",
  "name": "esbuild-wasm-benchmark",
  "main": "worker.ts",
  "compatibility_date": "2025-01-01",
  "compatibility_flags": ["nodejs_compat"],

  // Use rules to handle the WASM file
  "rules": [
    {
      "type": "CompiledWasm",
      "globs": ["**/*.wasm"]
    }
  ]
}
